spring:
  application:
    name: smartbank-management-system

  # Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/smart_bank
    username: root
    password: Y@gne&h_2911 
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: update  
    show-sql: true      
    properties:
      hibernate:
       format_sql: true  
      dialect: org.hibernate.dialect.MySQLDialect


  mail:
   host: smtp.gmail.com
   port: 587
   username: d.yagnesh555@gmail.com
   password: msyt cfny hydi afuo
   properties:
    mail:
     smtp:
       auth: true
       starttls:
         enable: true

server:
  port: 8080

logging:
   level:
     root: INFO
     com.bank.smartbank: DEBUG  # Your package logs at DEBUG level
     org.springframework.security: DEBUG  # Security logs
     org.hibernate.SQL: DEBUG  # SQL queries
   pattern:
     console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
   file:
      name: logs/smart-bank.log